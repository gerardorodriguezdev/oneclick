CREATE TABLE rooms (
    home_id VARCHAR(20) REFERENCES homes(home_id) ON DELETE CASCADE,
    room_id CHAR(36) PRIMARY KEY,
    room_name VARCHAR(20) NOT NULL
);

roomsByHomeId:
SELECT * FROM rooms WHERE home_id = ?;