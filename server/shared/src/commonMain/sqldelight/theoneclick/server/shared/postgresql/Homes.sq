CREATE TABLE homes (
    user_id CHAR(36) REFERENCES users(user_id) ON DELETE CASCADE,
    home_name VARCHAR(20) PRIMARY KEY
);

homesByUserId:
SELECT * FROM homes WHERE user_id = ? LIMIT ? OFFSET ?;

totalHomes:
SELECT COUNT(*) FROM homes;